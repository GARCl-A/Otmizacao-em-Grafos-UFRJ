#ifndef COMPARE_PAIRS_H
#define COMPARE_PAIRS_H

#include <vector>

using namespace std;

// Declaração da função para comparar pares
bool compare_pairs(
  const pair<float, int> &a, 
  const pair<float, int> &b
);

#endif // COMPARE_PAIRS_H